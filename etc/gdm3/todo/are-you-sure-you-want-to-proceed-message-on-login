https://askubuntu.com/questions/587868/are-you-sure-you-want-to-proceed-message-on-login

werner@X10DAi:/etc/gdm3$ ug zenity
Xsession:# Note that this should only go to zenity dialogs which always expect utf8
Xsession:zenity=`gdmwhich zenity`
Xsession:    if [ -n "$zenity" ] ; then
Xsession:      "$zenity" --info --text "$disptext"
Xsession:if [ -n "$zenity" ] ; then
Xsession:	"$zenity" --info --text "$disptext"
config-error-dialog.sh:elif [ -x /usr/bin/zenity ]; then
config-error-dialog.sh:	zenity --warning --no-wrap --text="$TEXT"

